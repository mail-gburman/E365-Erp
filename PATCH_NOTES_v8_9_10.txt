KPS ERP Enterprise v8.9.10

Fixes in this build:
- Frontend AuditPage syntax fix: corrected broken newline split regex that stopped Vite dev/build from starting.
- Audit readable details improved: permission updates are now summarized in plain business language instead of dumping raw JSON in UI/PDF/ZIP.
- Audit wrapping hardened for long text.
- Backend ORM warning fixed for service job attachments by linking relationships with back_populates.

Validation:
- Frontend production build passed in container.
- Backend Python files compile successfully.
